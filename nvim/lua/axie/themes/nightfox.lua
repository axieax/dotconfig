-- https://github.com/EdenEast/nightfox.nvim --

return function()
  local nightfox = require("nightfox")
  nightfox.setup({
    -- options = {
    -- transparent = true,
    -- dim_inactive = true,
    -- },
  })

  -- vim.cmd("colorscheme duskfox")
end
